# Researcher

The Researcher receives Telegram submissions, enriches them with web lookup when useful, and writes information-dense Markdown files into the shared `input/` directory.

Researcher prompt configuration lives in `prompts/researcher.yaml`.
